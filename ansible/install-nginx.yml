---
- name: Install Nginx
  hosts: web
  become: yes

  tasks:
    - name: Install Nginx
      ansible.builtin.dnf:
       name: nginx
       state: present

    - name: Start Nginx
      ansible.builtin.service:
       name: nginx
       state: started
       enabled: yes    
