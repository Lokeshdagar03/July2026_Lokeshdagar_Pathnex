pipeline {
    agent any

    stages {
        stage('Monitor') {
            steps {
                script {
                    echo 'Triggering CloudWatch alarms if CPU exceeds 80%'
                    sh 'echo Monitoring CPU'
                }
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }
    }
}