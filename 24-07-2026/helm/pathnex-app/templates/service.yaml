apiVersion: v1
kind: Service
metadata:
  name: {{ include "pathnex-app.fullname" . }}
  labels:
    {{- include "pathnex-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pathnex-app.selectorLabels" . | nindent 4 }}
