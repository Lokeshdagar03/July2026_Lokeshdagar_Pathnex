services:
  web:
    image: nginx
    container_name: web-app
    ports:
      - "8080:80"

  database:
    image: mysql:5.7
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: root123
    ports:
      - "3306:3306"
